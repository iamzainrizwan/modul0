# modul0

`shhh...`

portfolio + blog. astro + react, amoled + purple. there's a terminal hiding
in it (press `` ` ``).

```sh
npm i
npm run dev      # node >= 22.12
```

content lives in `src/data/profile.ts`. new post: add
`src/content/blog/<slug>.md` with `title`, `date`, `description` frontmatter.
